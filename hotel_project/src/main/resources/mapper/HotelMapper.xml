<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hotel_project.hotel_jpa.hotel.mapper.HotelMapper">

    <!-- 이름 검색 통합 (페이지네이션, 검색어 없으면 전체 조회) - INNER JOIN으로 도시 이름 포함 -->
    <select id="findByName" resultType="com.hotel_project.hotel_jpa.hotel.dto.HotelViewDto">
        SELECT
        h.id,
        h.hotel_name as hotelName,
        h.hotel_content as hotelContent,
        h.city_id as cityId,
        h.hotel_type as hotelType,
        h.hotel_latitude as hotelLatitude,
        h.hotel_longitude as hotelLongitude,
        h.hotel_star as hotelStar,
        h.hotel_number as hotelNumber,
        h.checkin_time as checkinTime,
        h.checkout_time as checkoutTime,
        h.created_at as createdAt,
        c.city_name as cityName
        FROM hotel_tbl h
        INNER JOIN city_tbl c ON h.city_id = c.id
        <where>
            <if test="HotelName != null and HotelName != ''">
                h.hotel_name LIKE CONCAT('%', #{HotelName}, '%')
                OR c.city_name LIKE CONCAT('%', #{HotelName}, '%')
            </if>
        </where>
        ORDER BY h.id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 개수 조회 (검색어 없으면 전체 개수) -->
    <select id="countByName" resultType="long">
        SELECT COUNT(*)
        FROM hotel_tbl h
        INNER JOIN city_tbl c ON h.city_id = c.id
        <where>
            <if test="HotelName != null and HotelName != ''">
                h.hotel_name LIKE CONCAT('%', #{HotelName}, '%')
                OR c.city_name LIKE CONCAT('%', #{HotelName}, '%')
            </if>
        </where>
    </select>

    <select id="getHotelBasicInfo" resultType="map">
        SELECT
            h.id as hotelId,
            h.hotel_name as hotelName,
            h.hotel_content as description,
            CONCAT(c.city_name, ', ', co.country_name) as address,
            c.city_name as cityName,
            co.country_name as countryName,
            h.hotel_latitude as latitude,
            h.hotel_longitude as longitude,
            h.hotel_number as phoneNumber,
            h.checkin_time as checkinTime,
            h.checkout_time as checkoutTime
        FROM hotel_tbl h
                 LEFT JOIN city_tbl c ON h.city_id = c.id
                 LEFT JOIN country_tbl co ON c.country_id = co.id
        WHERE h.id = #{hotelId}
    </select>

    <select id="getFirstHotelImage" resultType="string">
        SELECT hotel_image_path
            FROM hotel_image_tbl
            WHERE hotel_id = #{hotelId}
            ORDER BY created_at ASC
        LIMIT 1
    </select>

</mapper>