<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hotel_project.hotel_jpa.room.mapper.RoomMapper">

    <!-- 이름 검색 통합 (페이지네이션, 검색어 없으면 전체 조회) - INNER JOIN으로 호텔 이름 포함 -->
    <select id="findByName" resultType="com.hotel_project.hotel_jpa.room.dto.RoomViewDto">
        SELECT
        r.id,
        r.room_name as roomName,
        r.room_number as roomNumber,
        CAST(r.base_price AS UNSIGNED) as basePrice,
        CAST(r.room_single_bed AS UNSIGNED) as roomSingleBed,
        CAST(r.room_double_bed AS UNSIGNED) as roomDoubleBed,
        r.room_view as roomView,
        r.hotel_id as hotelId,
        h.hotel_name as hotelName
        FROM room_tbl r
        INNER JOIN hotel_tbl h ON r.hotel_id = h.id
        <where>
            <if test="RoomName != null and RoomName != ''">
                r.room_name LIKE CONCAT('%', #{RoomName}, '%')
                OR h.hotel_name LIKE CONCAT('%', #{RoomName}, '%')
            </if>
        </where>
        ORDER BY r.id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 개수 조회 (검색어 없으면 전체 개수) -->
    <select id="countByName" resultType="long">
        SELECT COUNT(*)
        FROM room_tbl r
        INNER JOIN hotel_tbl h ON r.hotel_id = h.id
        <where>
            <if test="RoomName != null and RoomName != ''">
                r.room_name LIKE CONCAT('%', #{RoomName}, '%')
                OR h.hotel_name LIKE CONCAT('%', #{RoomName}, '%')
            </if>
        </where>
    </select>

</mapper>