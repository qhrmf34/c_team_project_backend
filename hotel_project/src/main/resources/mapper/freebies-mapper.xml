<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hotel_project.hotel_jpa.freebies.mapper.FreebiesMapper">

    <select id="findById" parameterType="Long"
            resultType="com.hotel_project.hotel_jpa.freebies.dto.FreebiesDto">
        SELECT id, freebies_name as freebiesName
        FROM freebies_tbl
        WHERE id = #{id}
    </select>

    <select id="findAll"
            resultType="com.hotel_project.hotel_jpa.freebies.dto.FreebiesDto">
        SELECT id, freebies_name as freebiesName
        FROM freebies_tbl
        ORDER BY id DESC
    </select>

    <select id="findByNameContains"
            resultType="com.hotel_project.hotel_jpa.freebies.dto.FreebiesDto">
        SELECT id, freebies_name as freebiesName
        FROM freebies_tbl
        WHERE freebies_name LIKE CONCAT('%', #{freebiesName}, '%')
        ORDER BY id DESC
            LIMIT #{pageable.pageSize} + 1 OFFSET #{pageable.offset}
    </select>

</mapper>
