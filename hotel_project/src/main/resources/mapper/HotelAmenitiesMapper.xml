<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel_project.hotel_jpa.hotel_amenities.mapper.HotelAmenitiesMapper">

    <!-- 이름 검색 통합 (페이지네이션, 검색어 없으면 전체 조회) - INNER JOIN으로 호텔명, 편의시설명 포함 -->
    <select id="findByName" resultType="com.hotel_project.hotel_jpa.hotel_amenities.dto.HotelAmenitiesViewDto">
        SELECT
        ha.id,
        ha.hotel_id as hotelId,
        ha.amenities_id as amenitiesId,
        ha.created_at as createdAt,
        h.hotel_name as hotelName,
        a.amenities_name as amenitiesName
        FROM hotel_amenities_tbl ha
        INNER JOIN hotel_tbl h ON ha.hotel_id = h.id
        INNER JOIN amenities_tbl a ON ha.amenities_id = a.id
        <where>
            <if test="param1 != null and param1 != ''">
                h.hotel_name LIKE CONCAT('%', #{param1}, '%')
                OR a.amenities_name LIKE CONCAT('%', #{param1}, '%')
            </if>
        </where>
        ORDER BY ha.id DESC
        LIMIT #{param3} OFFSET #{param2}
    </select>

    <!-- 개수 조회 (검색어 없으면 전체 개수) -->
    <select id="countByName" resultType="long">
        SELECT COUNT(*)
        FROM hotel_amenities_tbl ha
        INNER JOIN hotel_tbl h ON ha.hotel_id = h.id
        INNER JOIN amenities_tbl a ON ha.amenities_id = a.id
        <where>
            <if test="param1 != null and param1 != ''">
                h.hotel_name LIKE CONCAT('%', #{param1}, '%')
                OR a.amenities_name LIKE CONCAT('%', #{param1}, '%')
            </if>
        </where>
    </select>

</mapper>