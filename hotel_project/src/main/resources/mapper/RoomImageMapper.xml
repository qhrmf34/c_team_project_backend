<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel_project.hotel_jpa.room_image.mapper.RoomImageMapper">

    <!-- 이름 검색 통합 (페이지네이션, 검색어 없으면 전체 조회) - INNER JOIN으로 객실명 포함 -->
    <select id="findByName" resultType="com.hotel_project.hotel_jpa.room_image.dto.RoomImageViewDto">
        SELECT
        ri.id,
        ri.room_image_name as roomImageName,
        ri.room_image_path as roomImagePath,
        ri.room_image_size as roomImageSize,
        ri.created_at as createdAt,
        ri.room_id as roomId,
        r.room_name as roomName,
        h.hotel_name as hotelName
        FROM room_image_tbl ri
        INNER JOIN room_tbl r ON ri.room_id = r.id
        INNER JOIN hotel_tbl h ON r.hotel_id = h.id
        <where>
            <if test="roomName != null and roomName != ''">
                r.room_name LIKE CONCAT('%', #{roomName}, '%')
                OR ri.room_image_name LIKE CONCAT('%', #{roomName}, '%')
                OR h.hotel_name LIKE CONCAT('%', #{roomName}, '%')
            </if>
        </where>
        ORDER BY ri.id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 개수 조회 (검색어 없으면 전체 개수) -->
    <select id="countByName" resultType="long">
        SELECT COUNT(*)
        FROM room_image_tbl ri
        INNER JOIN room_tbl r ON ri.room_id = r.id
        INNER JOIN hotel_tbl h ON r.hotel_id = h.id
        <where>
            <if test="roomName != null and roomName != ''">
                r.room_name LIKE CONCAT('%', #{roomName}, '%')
                OR ri.room_image_name LIKE CONCAT('%', #{roomName}, '%')
                OR h.hotel_name LIKE CONCAT('%', #{roomName}, '%')
            </if>
        </where>
    </select>

    <!-- 객실별 이미지 조회 (추가) -->
    <select id="findByRoomId" resultType="com.hotel_project.hotel_jpa.room_image.dto.RoomImageDto">
        SELECT
            id,
            room_id as roomId,
            room_image_name as roomImageName,
            room_image_path as roomImagePath,
            room_image_size as roomImageSize,
            created_at as createdAt
        FROM room_image_tbl
        WHERE room_id = #{roomId}
        ORDER BY created_at ASC
    </select>

</mapper>