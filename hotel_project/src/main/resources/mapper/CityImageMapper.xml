<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel_project.hotel_jpa.city_image.mapper.CityImageMapper">

    <!-- 이름 검색 통합 (페이지네이션, 검색어 없으면 전체 조회) - INNER JOIN으로 도시 이름 포함 -->
    <select id="findByName" resultType="com.hotel_project.hotel_jpa.city_image.dto.CityImageViewDto">
        SELECT
        ci.id,
        ci.city_image_name as cityImageName,
        ci.city_image_path as cityImagePath,
        ci.city_image_size as cityImageSize,
        ci.city_image_index as cityImageIndex,
        ci.created_at as createdAt,
        ci.city_id as cityId,
        c.city_name as cityName,
        c.city_content as cityContent,
        co.country_name as countryName
        FROM city_image_tbl ci
        INNER JOIN city_tbl c ON ci.city_id = c.id
        INNER JOIN country_tbl co ON c.country_id = co.id
        <where>
            <if test="cityName != null and cityName != ''">
                ci.city_image_name LIKE CONCAT('%', #{cityName}, '%')
                OR c.city_name LIKE CONCAT('%', #{cityName}, '%')
                OR co.country_name LIKE CONCAT('%', #{cityName}, '%')
            </if>
        </where>
        ORDER BY ci.id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 개수 조회 (검색어 없으면 전체 개수) -->
    <select id="countByName" resultType="long">
        SELECT COUNT(*)
        FROM city_image_tbl ci
        INNER JOIN city_tbl c ON ci.city_id = c.id
        INNER JOIN country_tbl co ON c.country_id = co.id
        <where>
            <if test="cityName != null and cityName != ''">
                ci.city_image_name LIKE CONCAT('%', #{cityName}, '%')
                OR c.city_name LIKE CONCAT('%', #{cityName}, '%')
                OR co.country_name LIKE CONCAT('%', #{cityName}, '%')
            </if>
        </where>
    </select>

</mapper>