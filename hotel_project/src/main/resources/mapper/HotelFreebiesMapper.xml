<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel_project.hotel_jpa.hotel_freebies.mapper.HotelFreebiesMapper">

    <!-- 이름 검색 통합 (페이지네이션, 검색어 없으면 전체 조회) - INNER JOIN으로 호텔명, 무료시설명 포함 -->
    <select id="findByName" resultType="com.hotel_project.hotel_jpa.hotel_freebies.dto.HotelFreebiesViewDto">
        SELECT
        ha.id,
        ha.hotel_id as hotelId,
        ha.freebies_id as freebiesId,
        ha.is_available as isAvailable,
        ha.created_at as createdAt,
        h.hotel_name as hotelName,
        f.freebies_name as freebiesName
        FROM hotel_freebies_tbl ha
        INNER JOIN hotel_tbl h ON ha.hotel_id = h.id
        INNER JOIN freebies_tbl f ON ha.freebies_id = f.id
        <where>
            <if test="param1 != null and param1 != ''">
                h.hotel_name LIKE CONCAT('%', #{param1}, '%')
                OR f.freebies_name LIKE CONCAT('%', #{param1}, '%')
            </if>
        </where>
        ORDER BY ha.id DESC
        LIMIT #{param3} OFFSET #{param2}
    </select>

    <!-- 개수 조회 (검색어 없으면 전체 개수) -->
    <select id="countByName" resultType="long">
        SELECT COUNT(*)
        FROM hotel_freebies_tbl ha
        INNER JOIN hotel_tbl h ON ha.hotel_id = h.id
        INNER JOIN freebies_tbl f ON ha.freebies_id = f.id
        <where>
            <if test="param1 != null and param1 != ''">
                h.hotel_name LIKE CONCAT('%', #{param1}, '%')
                OR f.freebies_name LIKE CONCAT('%', #{param1}, '%')
            </if>
        </where>
    </select>

</mapper>